# Screenshot captions (Devpost upload order, each <= 140 chars)

1. 01-demo-board.png
   Contradiction handled live: the new preference supersedes the old fact with an audit trail, and the board shows every engine decision.

2. 04-recall.png
   A brand new session: Engram recalls the stored preference under a hard token budget and the agent answers with it, unprompted.

3. 03-decay.png
   Timely forgetting: an unused memory decays below the retention floor and drops out of recall, preserved for audit, never deleted.

4. 02-session-a.png
   Where it started: session A stores the preference as a typed memory with an LLM-scored importance of 1.00.

5. 05-live-on-alibaba.png
   The same engine running live on Alibaba Cloud Function Compute at engram.orionbot.online.
